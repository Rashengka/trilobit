{templateType Trilobit\Tests\Double\Front\DemoDefaultTemplate}

{*
 * A product page of the module a build can be without.
 *}

{block content}
    <h1 data-testid="demo-heading">{$heading}</h1>

    {include trail}

    <p data-testid="demo-content" data-kind="product" data-content-id="{$contentId}" data-content-path="{$contentPath}">
        A product.
    </p>
{/block}

{define trail}
    <nav aria-label="Breadcrumb" data-testid="breadcrumbs">
        <ol>
            <li n:foreach="$breadcrumbs as $crumb" data-testid="breadcrumb">
                <a n:tag-if="!$crumb->isCurrent" href="{$crumb->url}">{$crumb->label}</a>
            </li>
        </ol>
    </nav>
{/define}
