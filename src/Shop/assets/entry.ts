/**
 * Shop's own bundle, loaded only on pages under Shop:Front - see
 * src/Shop/Presentation/Front/templates/@layout.latte. Nothing here yet
 * beyond the marker below; a build with Shop switched off never resolves
 * this file at all, because vite.config.ts only lists it as an entry point
 * when var/build/modules.json says Shop is enabled.
 */
export {};
