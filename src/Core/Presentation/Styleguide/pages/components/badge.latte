{templateType Trilobit\Core\Presentation\Styleguide\OverviewDefaultTemplate}

{* A short label attached to something else: plain, in the accent, and a state that asks for attention. *}

{import '../../templates/furniture.latte'}
{import '../../../components/badge.latte'}

{block sgPage}
    {embed block sgSection, component: $components['c-badge']}
        {block sgSectionBody}
            {specimen 'plain'}
                {include badge, label: 'Draft'}
            {/specimen}

            {specimen 'accent'}
                {include badge, label: 'In this build', variant: 'accent'}
            {/specimen}

            {specimen 'danger'}
                {include badge, label: 'Overdue', variant: 'danger'}
            {/specimen}
        {/block}
    {/embed}
{/block}
